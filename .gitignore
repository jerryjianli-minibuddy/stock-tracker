__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/
dist/
build/

.env
.DS_Store

# Research PDFs and Finder clippings (kept local; not published to GitHub)
reports/**/*.pdf
reports/**/*.textClipping

# Per-machine launch config for Claude Preview MCP (everything else under
# .claude/ — including commands/ — is checked in and shared).
.claude/launch.json
.claude/settings.local.json
